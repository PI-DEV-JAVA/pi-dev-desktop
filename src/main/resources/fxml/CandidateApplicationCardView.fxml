<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.geometry.Insets?>

<ScrollPane xmlns="http://javafx.com/javafx/17"
            xmlns:fx="http://javafx.com/fxml/1"
            fx:controller="talentos.pidev.controllers.CandidateApplicationCardController"
            fitToWidth="true"
            styleClass="card-scroll"
            style="-fx-background: #0F172A;">

    <VBox spacing="20" style="-fx-padding: 28;">

        <!-- Hero Header -->
        <VBox spacing="10" styleClass="candidate-header" style="-fx-padding: 30;">
            <Label text="🎯 Postuler à une offre d'emploi"
                   style="-fx-font-size: 28px; -fx-font-weight: bold; -fx-text-fill: #F1F5F9;"/>
            <Label text="Trouvez l'offre qui vous correspond et postulez en quelques clics"
                   style="-fx-font-size: 16px; -fx-text-fill: #64748B;"/>
        </VBox>

        <!-- Filter Bar -->
        <HBox spacing="14" styleClass="filter-panel" style="-fx-padding: 20;" alignment="CENTER_LEFT">
            <Label text="🔍" styleClass="search-icon"/>
            <TextField fx:id="searchField" promptText="Rechercher par titre, description..."
                       styleClass="search-field" HBox.hgrow="ALWAYS"/>

            <ComboBox fx:id="departmentFilter" prefWidth="180"/>
            <ComboBox fx:id="contractFilter" prefWidth="150"/>

            <Button text="🔄 Actualiser" onAction="#refreshOffers" styleClass="btn-info"/>

            <Label fx:id="totalOffersLabel" styleClass="total-label"/>
        </HBox>

        <!-- Offer Cards Container -->
        <ScrollPane fitToWidth="true" styleClass="card-scroll">
            <FlowPane fx:id="offersCardsContainer"
                      hgap="20"
                      vgap="20"
                      alignment="TOP_CENTER">
                <padding>
                    <Insets top="10" right="10" bottom="10" left="10"/>
                </padding>
            </FlowPane>
        </ScrollPane>

        <!-- Application Form (hidden by default) -->
        <VBox fx:id="applicationForm" spacing="22" styleClass="candidate-form"
              style="-fx-padding: 32;"
              visible="false">

            <HBox spacing="16" alignment="CENTER_LEFT">
                <Label text="📝" style="-fx-font-size: 32px;"/>
                <VBox spacing="4">
                    <Label text="Formulaire de candidature"
                           style="-fx-font-size: 24px; -fx-font-weight: bold; -fx-text-fill: #F1F5F9;"/>
                    <Label text="Remplissez vos informations pour postuler"
                           style="-fx-font-size: 14px; -fx-text-fill: #64748B;"/>
                </VBox>
            </HBox>

            <!-- Selected offer summary -->
            <VBox spacing="10" styleClass="selected-offer-panel" style="-fx-padding: 20;">
                <Label text="Offre sélectionnée :" style="-fx-font-weight: bold; -fx-text-fill: #94A3B8;"/>
                <Label fx:id="selectedOfferTitle"
                       style="-fx-font-size: 18px; -fx-font-weight: bold; -fx-text-fill: #E2E8F0;"/>
                <HBox spacing="20">
                    <Label fx:id="selectedOfferDept" style="-fx-text-fill: #94A3B8;"/>
                    <Label fx:id="selectedOfferLocation" style="-fx-text-fill: #94A3B8;"/>
                </HBox>
                <HBox spacing="20">
                    <Label fx:id="selectedOfferSalary" style="-fx-text-fill: #34D399; -fx-font-weight: bold;"/>
                    <Label fx:id="selectedOfferClosing" style="-fx-text-fill: #F87171;"/>
                </HBox>
            </VBox>

            <!-- Form fields -->
            <GridPane hgap="20" vgap="16">
                <columnConstraints>
                    <ColumnConstraints percentWidth="28"/>
                    <ColumnConstraints percentWidth="72"/>
                </columnConstraints>

                <Label text="Nom complet *" GridPane.columnIndex="0" GridPane.rowIndex="0" styleClass="form-label"/>
                <TextField fx:id="nameField" promptText="Votre nom et prénom"
                           GridPane.columnIndex="1" GridPane.rowIndex="0"/>

                <Label text="Email *" GridPane.columnIndex="0" GridPane.rowIndex="1" styleClass="form-label"/>
                <TextField fx:id="emailField" promptText="exemple@email.com"
                           GridPane.columnIndex="1" GridPane.rowIndex="1"/>

                <Label text="Téléphone" GridPane.columnIndex="0" GridPane.rowIndex="2" styleClass="form-label"/>
                <TextField fx:id="phoneField" promptText="+216 XX XXX XXX"
                           GridPane.columnIndex="1" GridPane.rowIndex="2"/>

                <Label text="CV *" GridPane.columnIndex="0" GridPane.rowIndex="3" styleClass="form-label"/>
                <VBox spacing="6" GridPane.columnIndex="1" GridPane.rowIndex="3">
                    <Button text="📎 Choisir un fichier" onAction="#chooseCV" styleClass="btn-secondary"/>
                    <Label fx:id="cvFileNameLabel" text="Aucun fichier sélectionné"
                           style="-fx-text-fill: #64748B; -fx-font-size: 12px;"/>
                </VBox>

                <Label text="Lettre de motivation" GridPane.columnIndex="0" GridPane.rowIndex="4"
                       styleClass="form-label"/>
                <TextArea fx:id="motivationArea"
                          promptText="Parlez-nous de votre motivation, de votre parcours..."
                          prefRowCount="5" wrapText="true"
                          GridPane.columnIndex="1" GridPane.rowIndex="4"/>
            </GridPane>

            <!-- Action buttons -->
            <HBox spacing="16" alignment="CENTER_RIGHT">
                <Button text="← Retour aux offres" onAction="#backToOffers" styleClass="btn-back"/>
                <Button fx:id="submitButton" text="📤 Envoyer ma candidature" onAction="#submitApplication"
                        styleClass="btn-gradient"/>
            </HBox>

            <Label text="* Champs obligatoires" style="-fx-text-fill: #475569; -fx-font-size: 12px;"/>
        </VBox>
    </VBox>
</ScrollPane>
